<!DOCTYPE html>
<html>
<head>
  <title>Home</title>
</head>
<body>
  <h1>Welcome <%= user ? user.name : 'Guest' %></h1>
  <% if (user) { %>
    <a href="auth/logout">Logout</a>
  <% } else { %>
    <a href="/login">Goto login</a>
  <% } %>
</body>
</html>
